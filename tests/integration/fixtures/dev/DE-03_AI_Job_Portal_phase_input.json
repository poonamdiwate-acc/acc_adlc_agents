{
  "_note": "Empty phase_input — structured_requirements is loaded from the shared folder (bs_docs/) via LLM extraction, or from git fallback. volume_estimates and nfr_constraints are optional and may be extracted alongside."
}
